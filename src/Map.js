import React, { useEffect } from 'react';
import './App.css';

const Map = ({ mapDimensions }) => {
  useEffect(() => {
    // console.log(mapDimensions);
  }, [mapDimensions]);

  return <div>MAP</div>;
};

export default Map;
